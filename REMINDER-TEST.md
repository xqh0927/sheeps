# 秘境消消乐 — 复现测试提醒

> **2026年7月4日深夜开发完成，7月5日上午10点复现测试**

---

## 测试环境

- **设备**：Android 真机 / 模拟器
- **服务器**：Cloudflare Workers（确保 `wrangler dev` 或已部署）
- **网络**：WiFi / 4G 均测

---

## 1. 关卡类型弹窗（PrepareGameDialog）

**覆盖功能**：盲盒关 / 封印关 / 休息关入场提示弹窗

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1.1 | 进入 Level 2（封印关） | 弹出封印关提示卡片，浅蓝紫背景 + secondary 色文字 |
| 1.2 | 进入 Level 3（盲盒关） | 弹出盲盒关提示卡片，暖桃色背景 + primary 色文字 |
| 1.3 | 进入 Level 5（休息关，levelId % 5 == 0） | 弹出休息关提示，卡牌数 80% 折扣 |
| 1.4 | 进入 Level 6（封印关，非休息非盲盒） | 弹出封印关提示 |
| 1.5 | 进入 Level 1（普通关） | 不弹任何特殊提示，直接进入游戏 |
| 1.6 | 弹窗点击「开始挑战」/ 确认按钮 | 正常进入游戏，不闪退 |
| 1.7 | 弹窗点击返回 / 取消 | 回到关卡选择页面 |

---

## 2. 灵动动画皮肤（WebP Animation）

**覆盖功能**：SKIN_KEAI（萌趣卡通）和 SKIN_DAIMENG（呆萌手绘）的动画 WebP 渲染

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 2.1 | 设置 → 皮肤 → 选择「萌趣卡通」 | TileView 中卡牌显示动画 WebP（动起来） |
| 2.2 | 进入游戏关卡，移动/消除卡牌 | 动画不卡顿，不掉帧 |
| 2.3 | 设置 → 皮肤 → 选择「呆萌手绘」 | 同理，动画正常 |
| 2.4 | 切换到非动画皮肤（如默认） | 卡牌显示静态图，无动画 |
| 2.5 | 商城皮肤预览 | ShopItemCard 封面取 tile_<skin>_1 正常显示 |
| 2.6 | 反复切换动画皮肤 ↔ 静态皮肤 | 无内存泄漏，不 OOM |

---

## 3. 主题切换

**覆盖功能**：6 套主题颜色在全局 UI 上的正确应用

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 3.1 | 设置 → 主题 → 切换每套主题 | 全局颜色变化：主菜单、关卡选择、游戏界面、设置页 |
| 3.2 | 检查 TileCardBase | 卡牌颜色随主题切换（接入 MaterialTheme.colorScheme） |
| 3.3 | 检查所有弹窗（登录、设置、商城等） | 无白色 / 灰色硬编码残留 |
| 3.4 | 暗色模式下切换主题 | 文字可读，对比度正常 |
| 3.5 | 检查 Canvas 绘制元素 | 颜色正确（使用 @Composable 级 `val cs = MaterialTheme.colorScheme`） |
| 3.6 | 保留游戏标识色检查 | 帝王金(0xFFCBAA6A)、赛博霓虹、封印色(0xFFF1C40F) 始终不变 |

---

## 4. 密码注册登录

**覆盖功能**：PBKDF2-SHA256 密码认证（5 个接口）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 4.1 | 登录页 → 切换到密码模式 | 显示手机号 + 密码输入框 |
| 4.2 | 新用户 → 点击注册 → 输入手机号 + 密码 | 注册成功，自动登录 |
| 4.3 | 已注册用户 → 输入正确密码 | 登录成功，跳转主菜单 |
| 4.4 | 输入错误密码 | 提示「密码错误」，不清空 token |
| 4.5 | 切换回验证码模式 | 验证码登录正常 |
| 4.6 | 登录后 → 修改密码（新旧密码流程） | 修改成功，旧密码失效 |
| 4.7 | 忘记密码 → 重置密码 | 验证码验证后重置成功 |

---

## 5. 强制设密

**覆盖功能**：验证码登录后强制绑定密码（不可跳过，完成后奖励 50 积分）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 5.1 | 无密码用户 → 验证码登录 | 登录成功立即弹出「设置密码」页面 |
| 5.2 | 点击返回 / 关闭 | 无法跳过，弹窗不消失 |
| 5.3 | 输入密码并确认 → 提交 | 设置成功，提示获得 50 积分奖励 |
| 5.4 | 已有密码用户 → 验证码登录 | 不弹出设密页，直接进入主菜单 |
| 5.5 | 设密完成后检查 hasPassword 字段 | API 返回 hasPassword: true |

---

## 6. 积分系统

**覆盖功能**：签到、任务进度、积分增减

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 6.1 | 每日签到 | 签到成功，SIGN_IN_ONCE 自动领取 10 积分 |
| 6.2 | 完成一局游戏 → 提交分数 | PLAY_ 前缀任务 progress 正确更新 |
| 6.3 | 检查积分余额 | 与 API 返回一致 |
| 6.4 | 多次完成游戏 | 累计积分不丢失 |
| 6.5 | 查看积分历史 / 明细 | 显示正确 |

---

## 7. 头像 & 昵称

**覆盖功能**：头像上传（压缩）、昵称修改、手机号只读

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 7.1 | 个人资料页 → 点击头像 → 从相册选择 | 上传成功，头像自动压缩（≤256KB Base64） |
| 7.2 | 个人资料页 → 点击头像 → 拍照 | 上传成功 |
| 7.3 | 上传超 512KB 的图片 | 拒绝上传或自动进一步压缩 |
| 7.4 | 修改昵称 → 保存 | 昵称更新成功，各处显示一致 |
| 7.5 | 查看手机号 | 只读，不可编辑 |
| 7.6 | 退出登录后重新登录 | 头像和昵称保持不变（Profile 接口返回 avatarUrl + nickname） |

---

## 8. 加密传输（AES-256-GCM）

**覆盖功能**：全量 `/api/` 请求加解密

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 8.1 | 开启 Debug 模式 → 查看网络日志 | logging 拦截器输出明文（方便调试） |
| 8.2 | 关闭 Debug → Charles/Wireshark 抓包 | 请求体和响应体均为密文 |
| 8.3 | 发起任意 API 请求 | 正常解密，业务逻辑不受影响 |
| 8.4 | 检查拦截器顺序 | language → auth → tokenRefresh → encryption → weakNetRetry → logging |
| 8.5 | Token 过期 → 自动刷新 | 刷新后的请求仍正确加密 |
| 8.6 | 弱网环境（2G/3G） | 重试机制生效，加密层不影响重试 |

---

## 回归检查清单

- [ ] 所有 8 项功能测试用例全部通过
- [ ] 无闪退 / ANR / 白屏
- [ ] 内存使用正常（无泄漏）
- [ ] 网络异常处理正常（断网提示、重连恢复）
- [ ] 服务器 API 返回码正常

---

*生成时间：2026-07-05 10:00*
